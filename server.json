{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.wyre-technology/freshdesk-mcp",
  "title": "Freshdesk",
  "description": "MCP server for Freshdesk — tickets, contacts, companies, agents, groups, and knowledge base.",
  "repository": {
    "url": "https://github.com/wyre-technology/freshdesk-mcp",
    "source": "github"
  },
  "version": "0.0.0",
  "websiteUrl": "https://github.com/wyre-technology/freshdesk-mcp",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/wyre-technology/freshdesk-mcp:0.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "FRESHDESK_DOMAIN",
          "description": "Freshdesk account subdomain — the part before .freshdesk.com (e.g. 'acme')",
          "isRequired": true,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "FRESHDESK_API_KEY",
          "description": "Freshdesk API key (found under Profile Settings in the Freshdesk portal)",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MCP_TRANSPORT",
          "description": "Transport mode for the server. Set to 'stdio' for local CLI use; the image defaults to 'http' for gateway hosting.",
          "isRequired": false,
          "default": "stdio",
          "format": "string"
        },
        {
          "name": "AUTH_MODE",
          "description": "Credential source: 'env' reads vars locally, 'gateway' expects header injection from the WYRE MCP Gateway.",
          "isRequired": false,
          "default": "env",
          "format": "string"
        },
        {
          "name": "LOG_LEVEL",
          "description": "Log verbosity: debug, info, warn, error",
          "isRequired": false,
          "default": "info",
          "format": "string"
        }
      ]
    }
  ]
}

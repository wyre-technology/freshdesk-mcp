
services:
  freshdesk-mcp:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: freshdesk-mcp:latest
    container_name: freshdesk-mcp-server
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Required Freshdesk credentials (set these in .env file)
      - FRESHDESK_DOMAIN=${FRESHDESK_DOMAIN}
      - FRESHDESK_API_KEY=${FRESHDESK_API_KEY}

      # Optional configuration
      - MCP_SERVER_NAME=${MCP_SERVER_NAME:-freshdesk-mcp}
      - MCP_SERVER_VERSION=${MCP_SERVER_VERSION:-1.0.0}

      # Logging configuration
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=production
      - MCP_TRANSPORT=http
      - MCP_HTTP_PORT=8080
    ports:
      - "8080:8080"
    volumes:
      # Mount logs directory for persistence
      - freshdesk-mcp-logs:/app/logs

    # Health check
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    # Resource limits for production use
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 128M

  # Development service
  freshdesk-mcp-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    image: freshdesk-mcp:dev
    container_name: freshdesk-mcp-dev
    working_dir: /app
    restart: "no"
    command: npm run dev
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=${LOG_LEVEL:-debug}
      - FRESHDESK_DOMAIN=${FRESHDESK_DOMAIN}
      - FRESHDESK_API_KEY=${FRESHDESK_API_KEY}
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
      - freshdesk-mcp-dev-logs:/app/logs
    stdin_open: true
    tty: true

volumes:
  freshdesk-mcp-logs:
    driver: local
  freshdesk-mcp-dev-logs:
    driver: local
